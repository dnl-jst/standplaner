{% extends 'base.html.twig' %}

{% block title %}Neuen Benutzer anlegen{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1><i class="bi bi-person-plus"></i> Neuen Benutzer anlegen</h1>
                <a href="{{ path('app_user_index') }}" class="btn btn-secondary">
                    <i class="bi bi-arrow-left"></i> Zurück zur Übersicht
                </a>
            </div>

            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <div class="card">
                        <div class="card-body">
                    {{ form_start(form) }}
                        <div class="mb-3">
                            {{ form_label(form.email) }}
                            {{ form_widget(form.email) }}
                            {{ form_errors(form.email) }}
                            <div class="form-text">
                                <i class="bi bi-info-circle"></i>
                                Die E-Mail-Adresse dient als Benutzername für die Anmeldung.
                            </div>
                        </div>

                        <div class="mb-3">
                            {{ form_label(form.roles) }}
                            <div class="mt-2">
                                {% for choice in form.roles %}
                                    <div class="form-check">
                                        {{ form_widget(choice, {'attr': {'class': 'form-check-input'}}) }}
                                        {{ form_label(choice, null, {'label_attr': {'class': 'form-check-label'}}) }}
                                    </div>
                                {% endfor %}
                            </div>
                            {{ form_errors(form.roles) }}
                            <div class="form-text">
                                <i class="bi bi-info-circle"></i>
                                <strong>Benutzer:</strong> Kann sich anmelden und das Dashboard nutzen.<br>
                                <strong>Administrator:</strong> Hat zusätzlich Zugriff auf alle Verwaltungsfunktionen.
                            </div>
                        </div>

                        <div class="mb-3">
                            {{ form_label(form.plainPassword.first) }}
                            {{ form_widget(form.plainPassword.first) }}
                            {{ form_errors(form.plainPassword.first) }}
                        </div>

                        <div class="mb-4">
                            {{ form_label(form.plainPassword.second) }}
                            {{ form_widget(form.plainPassword.second) }}
                            {{ form_errors(form.plainPassword.second) }}
                            <div class="form-text">
                                <i class="bi bi-shield-lock"></i>
                                Das Passwort muss mindestens 6 Zeichen lang sein.
                            </div>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a href="{{ path('app_user_index') }}" class="btn btn-secondary">
                                <i class="bi bi-x-circle"></i> Abbrechen
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check-circle"></i> Benutzer anlegen
                            </button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>

            <div class="card mt-4">
                <div class="card-header">
                    <i class="bi bi-lightbulb"></i> Hinweise
                </div>
                <div class="card-body">
                    <ul class="mb-0">
                        <li><strong>E-Mail-Adresse:</strong> Muss eindeutig sein und wird als Benutzername verwendet.</li>
                        <li><strong>Rollen:</strong> Mindestens eine Rolle muss ausgewählt werden.</li>
                        <li><strong>Passwort:</strong> Wird sicher verschlüsselt gespeichert.</li>
                        <li><strong>Administrator-Rolle:</strong> Gibt Zugriff auf alle Verwaltungsfunktionen inkl. Benutzerverwaltung.</li>
                    </ul>
                </div>
            </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
